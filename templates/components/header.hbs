<header>
    {{#if user}}
        <p>Logged in as: {{user.name}}</p>
        <a href="/logout">Logout</a>
    {{else}}
        <a href="/login">Login</a>
    {{/if}}
    <a href="/home">Homepage</a>
</header>